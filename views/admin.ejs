<%- include('header', { currentPage: 'admin', isLoggedIn: true }) %>

<div class="container mt-5">
  <h2>Admin - User Summaries</h2>
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>Username</th>
        <th>Balance</th>
      </tr>
    </thead>
    <tbody>
      <% summaries.forEach(s => { %>
        <tr>
          <td><%= s.username %></td>
          <td>৳<%= s.balance.toFixed(2) %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include('footer') %>